spring:
  cloud:
    config:
      #uri: http://localhost:7101
      name: microcloud-provider-product-hystrix
      label: master
      username: admin
      password: configbus
      discovery:        enabled: true
        service-id: MICROCLOUD-CONFIG-BUS-APPLICATION
  rabbitmq:
    host: localhost
    port: 5672    # RabbitMQ的监听端口
    username: guest  # 用户名
    password: guest # 密码
#    
eureka:  client:
#    register-with-eureka: false    service-url:      defaultZone: http://admin:eurekaconfig@localhost:7201/eureka